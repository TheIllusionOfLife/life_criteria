\documentclass[letterpaper]{article}
\usepackage{natbib,alifeconf}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{url}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{hyperref}

% Blind footnote macro (ALIFE template)
\makeatletter
\def\blfootnote{\xdef\@thefnmark{}\@footnotetext}
\makeatother

\title{Searching for an Eighth Criterion of Life:\\
A Falsifiable Framework and Two Null Results}

\author{Anonymous}

\begin{document}
\maketitle
\blfootnote{\textcopyright{} 2026 Massachusetts Institute of Technology.
Published under a Creative Commons Attribution 4.0 International (CC BY 4.0) license.
\textbf{Data/Code:} [REDACTED FOR BLIND REVIEW]}

\noindent\textbf{Submission type: Full Paper}

\begin{abstract}
The textbook seven criteria of life---cellular organization, metabolism,
homeostasis, growth, reproduction, response to stimuli, and
evolution---are widely used to characterize living systems, yet whether
they suffice to explain resilience under novel perturbations remains
open.  We present a falsifiable framework for evaluating candidate
eighth criteria, grounded in functional analogy: each criterion must be
dynamic, energy-consuming, and form feedback loops with existing
criteria.  Three legitimacy tests---orthogonality, non-reducibility, and
causal necessity---operationalize evaluation.  The framework's diagnostic
power is confirmed by large ablation effects across all seven existing
criteria ($d = 0.78$--$14.7$, all $p < 0.005$).
We test two candidates under three perturbation regimes (famine,
boom-bust, seasonal cycle; $n=30$ held-out seeds; paired Wilcoxon with
Holm--Bonferroni):
\textbf{Candidate~A} (within-lifetime learning via EMA-driven homeostatic
correction) and \textbf{Candidate~B} (collective kin-sensing).
In enabled-vs-baseline comparisons across all regimes, both yield
bounded null results: all $|d_z| < 0.44$, all adjusted $p \geq 0.07$,
with TOST confirming formal equivalence for the majority of comparisons
(SESOI $\pm 0.5$).  A population cap relaxation check (cap$=$400,
matched 10{,}000-step protocol) confirms Candidate~B's enabled-condition
null persists under improved kin-signal viability; one seasonal
sham-condition contrast exceeds SESOI ($d_z = 0.54$) and is retained as
a robustness caveat.  Exploratory legitimacy tests show neither candidate
adds incremental predictive validity ($\Delta R^2 < 0.001$) or
introduces non-reducible information ($\text{CV}\,R^2 > 0.88$).
These bounded null results---within tested demographic regimes---constrain
each candidate's effect below practical significance, while the validated
protocol provides a reusable template for future candidates.
\end{abstract}

%% ================================================================
\section{Introduction}

What makes a system life-like?  Introductory biology textbooks
commonly enumerate seven criteria: cellular organization, metabolism,
homeostasis, growth and development, reproduction, response to stimuli,
and evolution \citep{urry_2020_campbell}.  These criteria serve as a
practical checklist, yet whether they constitute a \emph{sufficient}
characterization of life---especially under novel or extreme
conditions---remains an open question
\citep{cleland_2002_defining, benner_2010_defining}.

Artificial life systems---Tierra \citep{ray_1991_approach},
Avida \citep{ofria_2004_avida}, Lenia \citep{chan_2019_lenia},
Flow-Lenia \citep{plantec_2023_flow}, and others
\citep{yaeger_1994_computational, heinemann_2008_alien,
barbieux_2024_coralai}---each implement subsets of life-like properties.
Theoretical frameworks from autopoiesis
\citep{maturana_1980_autopoiesis} to chemoton theory
\citep{ganti_2003_principles} emphasize integrating metabolic,
boundary, and informational subsystems \citep{ruizmirazo_2004_universal}.

Despite this rich landscape, and based on representative systems in the
ALife literature, comparatively few platforms report \emph{all seven}
criteria as interdependent, individually ablatable processes---and
fewer still ask whether the seven are \emph{sufficient}.
\citet{bedau_2000_open} identified open problems in ALife that implicitly
point beyond the seven, including the evolution of adaptive behavior
and the emergence of collective organization.
The question we address is methodological: \emph{how should one
rigorously test whether an additional criterion provides explanatory
power beyond the existing seven?}

We contribute:
(1)~a \textbf{falsifiable evaluation protocol} with three legitimacy tests
and formal equivalence testing (TOST);
(2)~\textbf{framework validation} confirming all seven existing criteria
produce large ablation effects ($d = 0.78$--$14.7$);
(3)~\textbf{rigorous null results} for two candidates---within-lifetime
learning/memory and collective kin-sensing---in enabled-vs-baseline
comparisons across three perturbation regimes ($n=30$ seeds, four
conditions, Holm--Bonferroni correction), with a population cap
relaxation ruling out observability artifacts (one seasonal sham caveat
noted);
(4)~\textbf{exploratory legitimacy tests} showing neither candidate adds
predictive validity ($\Delta R^2 < 0.001$) or introduces non-reducible
information (CV $R^2 > 0.88$); and
(5)~\textbf{mechanistic diagnoses} explaining each null.

Reporting null results is essential for cumulative science
\citep{rosenthal_1979_file, fanelli_2012_negative}.  By defining our
smallest effect size of interest (SESOI) \emph{a priori} and applying
formal equivalence testing \citep{lakens_2017_equivalence,
lakens_2018_sesoi}, we transform ``failure to reject $H_0$'' into
bounded null results.


%% ================================================================
\section{Methods}

\subsection{System Architecture}

Our simulator implements a hybrid two-layer architecture.  At the lower
level, \textbf{swarm agents} (10--50 per organism) inhabit a continuous
2D environment and maintain organism cohesion through local coordination.
At the upper level, \textbf{organisms} (10--50 per simulation) emerge
from coordinated agent groups and possess individual genomes, neural
controllers, and metabolic state.

Each of the seven biological criteria is realized as a \emph{functional
analogy}: a computational process that (a)~is a dynamic process
requiring sustained resource consumption, (b)~whose removal causes
measurable degradation of organism self-maintenance, and (c)~forms
feedback loops with at least one other criterion.  The seven criteria map to specific subsystems:
(1)~\emph{cellular organization} (active boundary maintenance via swarm
coordination),
(2)~\emph{metabolism} (graph-based transformation network),
(3)~\emph{homeostasis} (NN-regulated internal state),
(4)~\emph{growth} (seed-to-mature developmental program),
(5)~\emph{reproduction} (organism division when metabolically ready),
(6)~\emph{response to stimuli} (local sensory field $\to$ NN $\to$
behavior), and
(7)~\emph{evolution} (heritable genomes, mutation, differential survival).

The baseline and Candidate~A conditions use a fixed-topology neural
controller ($8 \to 16 \to 4$, 212 weights).  Candidate~B appends one
kin-sensing input ($9 \to 16 \to 4$, 228 weights).  Weights are
genome-encoded and evolved through tournament selection.  Key simulation
parameters are listed in
Table~\ref{tab:params}.  Each run runs for 10000 timesteps with
population sampled every 100 steps, and uses deterministic seeding (same
seed $+$ config $\to$ identical output).


\subsection{Seven-Criteria Validation}

Before testing new candidates, we validated the framework's diagnostic
power by ablating each of the seven criteria individually ($n = 30$
seeds, 2{,}000 steps, graph-based metabolism).
Figure~\ref{fig:7criteria} shows the results: all seven ablations
produce significant effects (Table~\ref{tab:7criteria}), with
Cohen's~$d$ ranging from 0.78 (evolution) to 14.7 (reproduction).
This confirms that the simulator implements each criterion as a
genuinely functional process whose removal causes measurable
degradation---a prerequisite for meaningful eighth-criterion testing.


\subsection{Extending to an Eighth Criterion}

To avoid the ``arbitrary add-on'' criticism, we require any candidate
eighth criterion to satisfy the same functional analogy conditions
(a)--(c) plus an additional condition specific to the candidate's
domain.  We define three \textbf{legitimacy tests}:

\begin{enumerate}
\item \textbf{Orthogonality} (incremental predictive validity): The
  candidate indicator must add $R^2$ to a regression predicting survival
  AUC beyond seven-criteria proxy signals.
\item \textbf{Non-reducibility}: The candidate-specific signal cannot be
  predicted from the seven-criteria proxies alone (low cross-validated
  $R^2$).
\item \textbf{Causal necessity}: Ablating the candidate causes
  measurable degradation under appropriate conditions.
\end{enumerate}

A candidate that fails any legitimacy test under a given regime receives
a \emph{regime-specific null}, not a universal rejection---the
candidate may succeed under different ecological conditions.


\subsection{Experimental Design}

For each candidate $\times$ regime, we run four conditions on $n = 30$
held-out seeds (100--129, disjoint from calibration set 0--99):
(1)~\textbf{baseline} (seven criteria, candidate off),
(2)~\textbf{enabled} (candidate active),
(3)~\textbf{ablated} (candidate zeroed, all else identical), and
(4)~\textbf{sham} (compute-matched random updates).

Three perturbation regimes stress the system beyond normal operating
conditions:
\begin{itemize}
\item \textbf{Famine}: Resource availability drops to near-zero at
  step~3{,}000, creating acute survival pressure.
\item \textbf{Boom-bust}: Cyclic resource availability with period
  2{,}500 steps, creating recurring bottlenecks.
\item \textbf{Seasonal cycle}: Predictable sinusoidal resource
  oscillation with period 1{,}000 steps (10 full cycles per run),
  alternating between baseline and zero resource regeneration.
  This regime provides \emph{learnable temporal structure}---the
  fairest test for Candidate~A's memory mechanism.
\end{itemize}

\paragraph{Statistical pipeline.}
Because all four conditions share the same 30 seeds, yielding paired
observations, our primary analysis uses the \textbf{Wilcoxon signed-rank
test} \citep{wilcoxon_1945_individual} on seed-matched AUC differences.
Holm--Bonferroni correction \citep{holm_1979_simple} is applied within
each candidate $\times$ regime family (3 comparisons per family: enabled,
ablated, and sham, each vs.\ baseline).  Effect sizes are reported as
paired Cohen's~$d_z$ (mean difference divided by standard deviation of
differences) with 95\% confidence intervals.  Mann--Whitney~$U$
\citep{mann_1947_test} and Cliff's~$\delta$
\citep{cliff_1993_dominance} are computed as secondary (unpaired)
analyses for comparison with prior work but are not reported for
primary survival outcome comparisons; all primary inferences rely
solely on the paired Wilcoxon statistics reported in Table~\ref{tab:results}.

\paragraph{Inference tiers.}
Throughout: \emph{primary} = paired Wilcoxon + $d_z$ + Holm--Bonferroni;
\emph{equivalence} = TOST vs.\ SESOI $\pm 0.5$; \emph{secondary}
(Mann--Whitney, Cliff's~$\delta$) computed but not reported here;
\emph{exploratory} (regression, pre-shock) = permutation $p$ without
family-wise correction, labelled explicitly.

\paragraph{Equivalence testing.}
We pre-specify a smallest effect size of interest (SESOI) of $|d| = 0.5$
(medium effect; $d_z = 0.5$ corresponds to approximately one
half-standard-deviation shift in survival AUC---large enough to alter
population viability outcomes) and apply \textbf{Two One-Sided Tests}
(TOST; \citealt{lakens_2017_equivalence}) against bounds $\pm 0.5$.
A comparison is equivalent to null when TOST $p < 0.05$ (90\% CI for
$d_z$ falls within $[-0.5, 0.5]$).


%% ================================================================
\section{Candidate A: Within-Lifetime Memory}

\subsection{Implementation}

Candidate~A adds a \textbf{memory-driven homeostatic correction} via a
2-element exponential moving average (EMA) tracking mean internal state:
$m_{t+1}^{(i)} = \alpha \cdot m_t^{(i)} + (1 - \alpha) \cdot
\bar{s}_t^{(i)}$,
where $\bar{s}_t^{(i)}$ is the mean of channel~$i$ across agents and
$\alpha \in (0,1)$ is a fixed decay constant.
Letting $g_\text{eff}^{(i)} = \max(0,\, g_\text{base} + g_\text{genome}^{(i)})$
and $\tau_\text{eff}^{(i)} = \operatorname{clamp}(\tau_\text{base} +
\tau_\text{genome}^{(i)},\, 0, 1)$, the correction each timestep is:
\begin{equation}
\Delta s_t^{(i)} = g_\text{eff}^{(i)} \cdot
  \bigl(\tau_\text{eff}^{(i)} - m_t^{(i)}\bigr) \cdot \Delta t,
\label{eq:ema}
\end{equation}
where $g_\text{base}$, $\tau_\text{base}$ are fixed baselines,
$g_\text{genome}^{(i)}$, $\tau_\text{genome}^{(i)}$ are genome-encoded
offsets (gains clamped $\geq 0$; targets clamped to $[0,1]$), and
$\Delta t$ is the simulation timestep.
Memory initializes to zero at birth; the NN architecture (212~weights) is
unchanged.

This satisfies functional analogy: it is dynamic (continuous EMA
updates), cleanly ablatable (skip correction), and couples with
homeostasis and response-to-stimuli.  The additional condition is
\textbf{experience-dependence}: performance should improve with
within-lifetime experience.  The \textbf{sham control} replaces EMA
with uniform random draws, preserving computational cost while
destroying temporal coherence \citep{soltoggio_2018_born}.

\subsection{Results}

Table~\ref{tab:results} summarizes outcomes across all experiment suites.

\subsubsection*{Normal conditions (no perturbation)}
Survival AUC shows no significant effect of memory: enabled vs.\
baseline $d_z = 0.086$ (95\% CI $[-0.29, 0.46]$, $p_\mathrm{adj} =
1.00$).  TOST confirms equivalence ($p_\mathrm{TOST} = 0.015$).
Ablated and sham conditions are similarly indistinguishable
($|d_z| < 0.07$, both TOST $p < 0.02$).  Extinction: 0\% across all
conditions.

\subsubsection*{Famine stress}
Post-shock AUC: $d_z = 0.040$ (CI $[-0.33, 0.41]$, $p_\mathrm{adj} =
1.00$, TOST $p = 0.009$).  Extinction: baseline 86.7\%, enabled 93.3\%.
Memory provides no survival advantage under acute resource deprivation.

\subsubsection*{Boom-bust stress}
Survival AUC: $d_z = -0.193$ (CI $[-0.57, 0.18]$, $p_\mathrm{adj} =
0.35$, TOST $p = 0.052$).  The negative trend---sham and ablated
conditions trending better than enabled ($d_z = -0.32$ for sham)---suggests
that memory correction may \emph{destabilize} homeostasis under cyclic
stress, though no comparison reaches significance.

\subsubsection*{Seasonal cycle stress}
The seasonal regime provides the fairest test for memory: predictable
period-1{,}000 oscillations create learnable temporal structure that an
EMA mechanism should, in principle, track.  Survival AUC: enabled vs.\
baseline $d_z = 0.26$ (CI $[-0.12, 0.64]$, $p_\mathrm{adj} = 0.49$,
TOST $p = 0.10$).  Extinction: 0\% across all conditions---the regime
stresses organisms without catastrophic collapse.  Learning curve slope
(regression of per-cycle survival on cycle number): $-1.25$ (enabled)
vs.\ $-1.36$ (baseline), confirming no within-lifetime improvement
across successive seasonal cycles.  Per-cycle survival \emph{declines}
over time in all conditions, indicating that the EMA cannot convert
temporal predictability into survival advantage.

\subsection{Mechanism Verification}

Despite the null survival effect, the memory mechanism operates correctly
(Figure~\ref{fig:ema}).  EMA late variance is significantly lower for
enabled ($\bar{\sigma}^2 = 1.04 \times 10^{-4}$) than sham
($7.29 \times 10^{-4}$; $U = 33$, $p < 10^{-9}$).  Under stress,
memory variance \emph{increases} relative to sham ($3\times$ under
famine), indicating faithful tracking of environmental turbulence that
nonetheless destabilizes the correction signal.  The memory encodes
experience, but evolution does not select for memory-contingent behaviors.

Examining evolved genome parameters confirms this: population-mean memory
gain offsets ($g_\text{genome}^{(i)}$, Eq.~\ref{eq:ema}) converge near
near zero (mean $|\bar{g}| = 0.0015 \pm 0.0024$, $\max = 0.0097$, $n=30$ seeds) across held-out seeds, indicating that evolution
selectively ignores the correction channel rather than attempting to use it.


%% ================================================================
\section{Candidate B: Collective Kin-Sensing}

\subsection{Implementation}

Inspired by inclusive fitness theory \citep{hamilton_1964_genetical},
Candidate~B adds a \textbf{kin-sensing channel}: a single-pass neighbor
count computes \texttt{kin\_fraction} $\in [0, 1]$ (proportion of
nearby kin), which enters the NN as input channel~8 (9 inputs, 228
weights).  This is dynamic (recomputed each timestep), cleanly ablatable
(\texttt{kin\_fraction} $= 0$), and couples with cellular organization
and reproduction.  The \textbf{sham} permutes real
\texttt{kin\_fraction} values across agents, preserving the distribution
while destroying spatial correlation.

\subsection{Results}

\subsubsection*{Famine stress}
Post-shock AUC: $d_z = -0.141$ (CI $[-0.52, 0.23]$, $p_\mathrm{adj} =
0.64$, TOST $p = 0.030$).  Extinction: 96.7\% enabled vs.\ 86.7\%
baseline.

\subsubsection*{Boom-bust stress}
Survival AUC: $d_z = -0.065$ (CI $[-0.44, 0.31]$, $p_\mathrm{adj} =
1.00$, TOST $p = 0.012$).  Extinction: 76.7\% enabled vs.\ 83.3\%
baseline.  Enabled and sham conditions reach formal equivalence
($p_\mathrm{TOST} \leq 0.015$); ablated falls within SESOI but below
threshold ($p_\mathrm{TOST} = 0.080$).

\subsubsection*{Pre-shock window analysis}
To test whether Candidate~B provides benefit before demographic collapse
eliminates the kin signal, we analyzed the pre-shock phase (steps
0--3{,}000) separately.  During this window, organisms maintain larger
swarms (kin fraction $\sim$0.33), yet the effect remains null: $d_z =
0.22$ (famine, $p_\mathrm{adj} = 0.59$), $d_z = 0.31$ (boom-bust,
$p_\mathrm{adj} = 0.33$).

\subsection{Signal Sparsity Diagnosis}

No detected benefit from Candidate~B's kin-sensing mechanism was found
under the tested demographic regimes---an \emph{ecology-conditional} null,
not a class-level dismissal of collective organization.  The mechanistic
explanation (Figure~\ref{fig:kin}): \textbf{organisms converge to
approximately one agent each} under the default population cap
($\texttt{max\_alive\_organisms} = 100$).  When organisms consist of a
single agent, \texttt{kin\_fraction} $\approx 0$.  Mean
\texttt{kin\_fraction} at simulation end: 0.03 under famine, 0.19 under
boom-bust (driven by rare multi-agent survivors).

This is an \emph{observability failure}: the kin signal exists in
principle but becomes degenerate under the tested demographic regime.

\subsubsection*{Population cap relaxation (cap\,$=$\,400 vs.\ 100)}

To test whether Candidate~B's null is an artifact of the demographic
constraint, we relaxed \texttt{max\_alive\_organisms} from 100 to 400.
Under cap$=$400, mean \texttt{kin\_fraction} increased substantially
(0.03$\to$0.24 famine, 0.19$\to$0.14 boom-bust, $\to$0.22 seasonal),
confirming signal observability was restored.  Despite improved signal,
enabled-vs-baseline effects remain bounded across all three regimes
(largest $|d_z| = 0.23$, $p_\mathrm{adj} \ge 0.30$)---the same
qualitative outcome as cap$=$100 (Table~\ref{tab:results}, cap$=$400
block).  One caveat: the seasonal \emph{sham} arm exceeds SESOI
($d_z = 0.54$, $p_\mathrm{adj} = 0.004$); this applies to the sham
contrast only, not the enabled-condition primary result.


%% ================================================================
\section{Legitimacy Tests}

Beyond causal necessity (ablation), we conducted exploratory
orthogonality and non-reducibility analyses using existing experiment
data.

\subsection{Orthogonality: Incremental Predictive Validity}

We extracted seven proxy features from the late simulation window
(energy, waste, boundary integrity, genome diversity, spatial cohesion,
generation depth, genome drift) and fit OLS regressions predicting
survival AUC.  Adding the 8th-candidate binary indicator produced
negligible improvement:

\begin{itemize}
\item Candidate~A: $\Delta R^2 = 0.0008$ ($F = 0.83$, $p = 0.36$;
  permutation $p = 0.37$; CV $\Delta R^2 = -0.001$)
\item Candidate~B: $\Delta R^2 = 0.0008$ ($F = 0.39$, $p = 0.53$;
  permutation $p = 0.53$; CV $\Delta R^2 = -0.007$)
\end{itemize}

Bootstrap 95\% CIs for $\Delta R^2$ span $[0, 0.008]$ (Candidate~A)
and $[0, 0.012]$ (Candidate~B).  Cross-validated $\Delta R^2$ is
negative for both, indicating the candidate indicator adds noise
rather than signal.

\subsection{Non-Reducibility}

We tested whether each candidate's characteristic signal can be
predicted from the seven-criteria proxies alone.  High cross-validated
$R^2$ indicates the candidate signal is \emph{reducible} (predictable
from existing criteria):

\begin{itemize}
\item Candidate~A (memory late-window variance): CV $R^2 = 0.91$
  (permutation $p < 0.001$).  Interpretation: \textbf{reducible}.
\item Candidate~B (kin\_fraction): CV $R^2 = 0.88$ (permutation
  $p < 0.001$).  Interpretation: \textbf{reducible}.
\end{itemize}

Both candidate signals are almost entirely explained by existing
criteria.  Note that signal reducibility does not logically preclude
non-reducible \emph{survival effects}---a candidate might still provide
benefits not captured by linear proxy prediction.  Under our tested
conditions, neither candidate shows such decoupling: survival effects are
null and signals are reducible, making a latent non-reducible benefit
implausible.  These analyses are labelled exploratory due to sensitivity
to proxy feature construction.


%% ================================================================
\section{Discussion}

\subsection{Interpreting the Bounded Null Results}

In enabled-vs-baseline comparisons across all tested regimes, no
detectable survival benefit was found for either candidate---including
the seasonal regime designed as the fairest test for Candidate~A's EMA
memory.  Enabled paired effects are small in both primary 10{,}000-step
experiments and the cap$=$400 robustness check (Table~\ref{tab:results};
Figure~\ref{fig:forest}).  TOST confirms formal equivalence for the
majority of primary comparisons ($p_\mathrm{TOST} < 0.05$); one
cap$=$400 seasonal sham comparison lies outside SESOI and is treated as
a robustness caveat, not a candidate gain.

Importantly, these are \emph{diagnosed} nulls with distinct mechanisms.
\textbf{Candidate~A}: the EMA mechanism operates correctly and encodes
sensory history, but the evolutionary landscape does not select for
memory-contingent behaviors; under stress, memory destabilizes
homeostasis (variance 3$\times$ higher than sham under famine).
\textbf{Candidate~B}: demographic convergence to single-agent organisms
makes kin-fraction degenerate under cap$=$100; restoring viable kin
fractions (cap$=$400, kin-fraction 0.14--0.24) still yields no
enabled-condition benefit.

The exploratory legitimacy tests are consistent with these diagnoses:
neither candidate adds predictive validity ($\Delta R^2 < 0.001$, see
Table~\ref{tab:results}), and both signals are reducible to existing
criteria (CV $R^2 > 0.88$).

One testable hypothesis for the seasonal sham exception (cap$=$400) is
that random kin-fraction noise assists homeostatic search under periodic
structure without requiring accurate kin information; targeted follow-up
varying noise injection point would disambiguate this from a compute-cost
artifact.  More broadly, these bounded nulls reflect three specific
perturbation regimes and a fixed NN topology---they do not exclude that
alternative implementations or richer ecologies could yield positive
results for similar candidate classes.

\subsection{Candidate Space and Design Implications}

Our two candidates represent distinct branches of a broader taxonomy
(Figure~\ref{fig:taxonomy}): \emph{within-lifetime adaptation}
(Candidate~A) and \emph{collective organization} (Candidate~B).
Untested categories include anticipatory regulation (predictive coding,
immune-like recognition) and alternative implementations (synaptic
plasticity, niche construction).

Three design lessons emerge.
\textbf{Signal viability precedes signal utility}: Candidate~B's
kin signal becomes degenerate under ecological dynamics that eliminate
multi-agent organisms.
\textbf{Perturbation structure does not rescue learning}: the seasonal
regime provides repeatable temporal structure an EMA should track, yet
memory yields no survival advantage---the evolutionary landscape does
not select for memory-contingent behaviors.
\textbf{Mechanism verification is essential}: without the EMA convergence
test and kin-fraction analysis, nulls would be uninterpretable.


\subsection{Limitations}

\textbf{Regime and system scope.}  Our three regimes (famine, boom-bust,
seasonal) span acute, cyclic, and predictable stress; candidates may
succeed under spatial heterogeneity or multi-resource competition.
Different substrate architectures may support different mechanisms more
naturally.

\textbf{Population caps and controller capacity.}  The default cap
(100) drives convergence to single-agent organisms; our cap$=$400
relaxation increased kin-signal viability but one seasonal sham
comparison exceeded SESOI, warranting control-focused follow-up.
Higher population caps remain untested for Candidate~A; resource
competition dynamics at larger scales may alter the memory mechanism's
selective value.  The fixed-topology NN ($8/9 \to 16 \to 4$) may lack
capacity to exploit memory or kin information; topology evolution
\citep{stanley_2002_evolving} could change the outcome.

\textbf{Validation scope.}  The 7-criteria validation uses 2{,}000-step
runs versus 10{,}000 for 8th-criterion tests.  Given that all
7-criteria ablation effects are extremely large ($d \geq 0.78$, with
five of seven exceeding $d = 3.0$), these effects are expected to grow further
at 10{,}000 steps as evolutionary divergence accumulates---making the
step-count difference inconsequential for framework validation.
Orthogonality and non-reducibility analyses are exploratory, and the
regression approach assumes linearity; alternative proxy feature sets
may yield different reducibility estimates.


\subsection{What Would Count as Success}

A successful candidate must: achieve $|d_z| \geq 0.5$ on survival AUC
across $\geq 2$ regimes; show significant ablation degradation
($p_\mathrm{adj} < 0.05$); outperform its sham control; add significant
$\Delta R^2$ beyond seven-criteria proxies; resist prediction from
existing criteria (CV $R^2 < 0.5$); and pass mechanism verification.


%% ================================================================
\section{Conclusion}

We presented a falsifiable framework for evaluating candidate eighth
criteria of life-likeness, incorporating paired statistical analysis,
formal equivalence testing, and three legitimacy tests.  Framework
diagnostic power is confirmed by large ablation effects across all seven
existing criteria (Table~\ref{tab:7criteria}; Figure~\ref{fig:7criteria}).
We then tested two candidates across three perturbation regimes and a
population cap robustness check.  In enabled-vs-baseline comparisons
across all tested regimes, both yield bounded null results, with TOST
confirming formal equivalence for the majority of comparisons (one
seasonal sham caveat at cap$=$400 noted).  Exploratory legitimacy tests
confirm neither candidate adds incremental predictive validity or
introduces non-reducible information (Table~\ref{tab:results}).

The contribution is fourfold: (1)~the evaluation protocol is reusable
for future candidates; (2)~the 7-criteria validation demonstrates the
system's diagnostic power; (3)~the diagnosed nulls---distinct mechanisms
for each candidate---constrain the design space; and (4)~explicit null
reporting contributes to the cumulative record
\citep{rosenthal_1979_file, fanelli_2012_negative}.

The search for what, if anything, lies beyond the textbook seven criteria
remains open.  Our framework provides a systematic, falsifiable approach
to that search.


%% ================================================================

% Parameter table
\begin{table}[t]
\centering
\caption{Key simulation parameters (from tuned baseline).}
\label{tab:params}
\small
\begin{tabular}{@{}lr@{}}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Organisms (initial / max) & 10 / 100 \\
Agents per organism & 10--50 \\
NN topology & $8 \to 16 \to 4$ (212 wt.) \\
Simulation steps & 10{,}000 \\
Sample interval & 100 steps \\
Resource regen.\ rate & 0.01 \\
Boundary decay rate & 0.001 \\
Boundary repair rate & 0.05 \\
Homeostasis decay rate & 0.01 \\
Maturation steps & 200 \\
Metabolism mode & graph \\
Seeds (test set) & 100--129 ($n=30$) \\
\bottomrule
\end{tabular}
\end{table}

% 7-criteria ablation table
\begin{table}[t]
\centering
\caption{Seven-criteria single-ablation results ($n=30$, 2{,}000 steps).
All effects significant after Holm--Bonferroni correction ($p < 0.005$).}
\label{tab:7criteria}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Ablated Criterion} & \textbf{$|d|$} & \textbf{$p_\mathrm{corr}$} \\
\midrule
Reproduction    & 14.72 & $< 10^{-10}$ \\
Response        & 14.29 & $< 10^{-10}$ \\
Metabolism      & 14.10 & $< 10^{-10}$ \\
Growth          &  6.38 & $< 10^{-10}$ \\
Boundary        &  3.41 & $< 10^{-10}$ \\
Homeostasis     &  3.03 & $< 10^{-10}$ \\
Evolution       &  0.78 & $4.7 \times 10^{-3}$ \\
\bottomrule
\end{tabular}
\end{table}

% Main results table
\begin{table*}[t]
\centering
\caption{Summary of results across all experiment suites.  $d_z$ is
paired Cohen's~$d$ (primary).  $p_\mathrm{adj}$ is Holm--Bonferroni
adjusted Wilcoxon $p$-value.  $p_\mathrm{TOST}$ is TOST equivalence
$p$-value against SESOI $= \pm 0.5$.  Ext.\ is extinction rate.
Bold TOST indicates formal equivalence ($p < 0.05$).}
\label{tab:results}
\small
\begin{tabular}{@{}llrrcrl@{}}
\toprule
\textbf{Candidate} & \textbf{Regime} &
\textbf{$d_z$} & \textbf{95\% CI} & \textbf{$p_\mathrm{adj}$} &
\textbf{$p_\mathrm{TOST}$} & \textbf{Ext.\ (\%)} \\
\midrule
\multicolumn{7}{@{}l}{\textit{Candidate A: Memory (EMA)}} \\
\quad Enabled & Normal  & $0.09$  & $[-0.29, 0.46]$ & $1.00$ & $\mathbf{0.015}$ & 0 \\
\quad Ablated & Normal  & $0.06$  & $[-0.31, 0.44]$ & $1.00$ & $\mathbf{0.012}$ & 0 \\
\quad Sham    & Normal  & $0.06$  & $[-0.31, 0.43]$ & $1.00$ & $\mathbf{0.011}$ & 0 \\
\addlinespace
\quad Enabled & Famine  & $0.04$  & $[-0.33, 0.41]$ & $1.00$ & $\mathbf{0.009}$ & 93.3 \\
\quad Ablated & Famine  & $0.11$  & $[-0.27, 0.48]$ & $1.00$ & $\mathbf{0.020}$ & 76.7 \\
\quad Sham    & Famine  & $0.27$  & $[-0.11, 0.65]$ & $0.55$ & $0.108$          & 80.0 \\
\addlinespace
\quad Enabled & Boom-bust & $-0.19$ & $[-0.57, 0.18]$ & $0.35$ & $0.052$ & 93.3 \\
\quad Ablated & Boom-bust & $-0.22$ & $[-0.60, 0.16]$ & $0.35$ & $0.069$ & 70.0 \\
\quad Sham    & Boom-bust & $-0.32$ & $[-0.71, 0.06]$ & $0.19$ & $0.169$ & 66.7 \\
\addlinespace
\quad Enabled & Seasonal & $0.26$  & $[-0.12, 0.64]$ & $0.49$ & $0.102$ & 0 \\
\quad Ablated & Seasonal & $0.33$  & $[-0.05, 0.71]$ & $0.42$ & $0.180$ & 0 \\
\quad Sham    & Seasonal & $0.24$  & $[-0.14, 0.62]$ & $0.49$ & $0.080$ & 0 \\
\midrule
\multicolumn{7}{@{}l}{\textit{Candidate B: Kin-Sensing}} \\
\quad Enabled & Famine  & $-0.14$ & $[-0.52, 0.23]$ & $0.64$ & $\mathbf{0.030}$ & 96.7 \\
\quad Ablated & Famine  & $-0.18$ & $[-0.56, 0.20]$ & $0.53$ & $\mathbf{0.045}$ & 86.7 \\
\quad Sham    & Famine  & $0.11$  & $[-0.27, 0.48]$ & $0.64$ & $\mathbf{0.020}$ & 80.0 \\
\addlinespace
\quad Enabled & Boom-bust & $-0.07$ & $[-0.44, 0.31]$ & $1.00$ & $\mathbf{0.012}$ & 76.7 \\
\quad Ablated & Boom-bust & $0.24$  & $[-0.14, 0.61]$ & $0.53$ & $0.080$ & 86.7 \\
\quad Sham    & Boom-bust & $0.08$  & $[-0.29, 0.45]$ & $1.00$ & $\mathbf{0.015}$ & 80.0 \\
\addlinespace
\quad Enabled & Seasonal & $0.19$  & $[-0.19, 0.56]$ & $0.90$ & $\mathbf{0.048}$ & 0 \\
\quad Ablated & Seasonal & $0.29$  & $[-0.09, 0.67]$ & $0.40$ & $0.133$ & 0 \\
\quad Sham    & Seasonal & $0.10$  & $[-0.27, 0.48]$ & $0.90$ & $\mathbf{0.019}$ & 0 \\
\midrule
\multicolumn{7}{@{}l}{\textit{Candidate B: Kin-Sensing (cap$=$400)}} \\
\quad Enabled & Famine  & $0.23$  & $[-0.15, 0.61]$ & $0.76$ & $0.074$ & 86.7 \\
\quad Enabled & Boom-bust & $0.22$  & $[-0.16, 0.59]$ & $0.95$ & $0.066$ & 100.0 \\
\quad Enabled & Seasonal & $0.23$  & $[-0.15, 0.61]$ & $0.30$ & $0.073$ & 0 \\
\bottomrule
\end{tabular}
\end{table*}

% Figures
\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_7criteria_ablation.pdf}
\caption{Seven-criteria ablation: Cohen's~$d$ for each single-criterion
ablation vs.\ normal baseline.  All 7 significant ($d = 0.78$--$14.7$),
demonstrating framework diagnostic power.}
\label{fig:7criteria}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_forest_plot.pdf}
\caption{Forest plot: paired Cohen's~$d_z$ with 95\% CIs for all
8th-criterion comparisons.  Green shading: SESOI region ($\pm 0.5$).
Green points: CI falls within SESOI (formal equivalence).}
\label{fig:forest}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_ema_dynamics.pdf}
\caption{EMA memory dynamics.  (A)~Memory trace converges for enabled
condition but not sham.  (B)~Late-window variance by regime: memory
stabilizes under normal conditions but destabilizes under stress.}
\label{fig:ema}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_kin_fraction_timeseries.pdf}
\caption{Kin fraction signal degeneration.  (A)~Kin fraction collapses
post-shock as organisms converge to single agents.  (B)~Cap comparison
shows signal dependence on population carrying capacity.}
\label{fig:kin}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_candidate_space.pdf}
\caption{Candidate space taxonomy.  Solid boxes: tested candidates
(both null).  Dashed boxes: untested alternatives for future work.}
\label{fig:taxonomy}
\end{figure}



\footnotesize
\bibliographystyle{apalike}
\bibliography{references}

\end{document}
